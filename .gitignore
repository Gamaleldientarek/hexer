# brainstorming session artefacts (local mockups)
.superpowers/

# dependencies
node_modules/

# build / release
*.zip
dist/

# test output
playwright-report/
test-results/
coverage/

# macOS
.DS_Store
# NB: do not add the usual "Icon?" pattern here. On a case-insensitive
# filesystem it also matches the icons/ directory, which must be committed.

# Google Drive sync junk (this folder lives in Drive)
.tmp.drivedownload/
.tmp.driveupload/

# editors
.vscode/
.idea/
*.swp
.design/
